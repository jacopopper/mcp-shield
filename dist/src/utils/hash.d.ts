/**
 * Simple SHA-256 hash for audit trail.
 */
export declare function hash(data: string): string;
/**
 * Generate a short hash (first 8 chars) for IDs.
 */
export declare function shortHash(data: string): string;
/**
 * Generate a unique ID for audit entries.
 */
export declare function generateAuditId(): string;
//# sourceMappingURL=hash.d.ts.map